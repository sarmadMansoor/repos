#include <iostream>
#include "Commission.h"
using namespace std;
//CommissionEmployee::CommissionEmployee(
//	const String& first, const String& last, const String& ssn,
//	double sales, double rate) :lastName(last), firstName(first), socialSecurityNumber(ssn)
//{}
